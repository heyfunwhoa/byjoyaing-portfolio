# Create a free API key at https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxx

# Inbox that receives contact messages and resume requests
CONTACT_INBOX=kristen.aing@gmail.com

# Optional alias if CONTACT_INBOX is unset
RESUME_INBOX=kristen.aing@gmail.com

# Must be a verified sender in Resend.
# Use onboarding@resend.dev only for tests (it can only send to your Resend account email).
RESEND_FROM="Kristen Joy Aing <onboarding@resend.dev>"
